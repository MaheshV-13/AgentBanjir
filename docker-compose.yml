version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: agentbanjir-postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: hackathonpassword
      POSTGRES_DB: agentbanjir
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: